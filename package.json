{
  "name": "clarion-csv-editor-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test tooling for the Clarion CSV Editor addin (front-end).",
  "scripts": {
    "test:unit": "node --test tests/grid.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
